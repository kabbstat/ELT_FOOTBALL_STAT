[tool.poetry]
name = "elt"
version = "0.1.0"
description = "Football data extraction to GCP"
authors = ["Your Name <statkabb95@gmail.com>"]
package-mode = false

[tool.poetry.dependencies]
python = "^3.10"
google-cloud-storage = "^2.14.0"
pyarrow = "^15.0.0"
pandas = "^2.2.0"
httpx = "^0.27.0"
python-dotenv = "^1.0.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"